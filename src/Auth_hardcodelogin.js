// The Auth component is bypassed and directly renders the ChoreManagementApp component
// This is for demonstration purposes only
const Auth = () => {
    // Bypass authentication and directly render ChoreManagementApp
    return <ChoreManagementApp />;
  };

export default Auth;